name: Windows VSDX packages

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: windows-vsdx-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows:
    runs-on: windows-2022
    timeout-minutes: 355
    env:
      # NSPR's Cygwin wrapper misdetects MSYS2's root mount as a drive prefix
      # and rewrites runner paths such as D:/a/... to D:a:/.... Use a prefix
      # that MSYS2 path conversion leaves alone; the wrapper then passes the
      # already-native path to MSVC unchanged.
      CYGDRIVE_MOUNT: /cygdrive

    steps:
      - name: Preserve LF line endings
        shell: pwsh
        run: git config --global core.autocrlf input

      - name: Check out the VSDX branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          filter: blob:none
          persist-credentials: false

      - name: Install Visual C++ ATL
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $installer = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\setup.exe"
          $installationPath = & $vswhere -latest -products '*' -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          if (-not $installationPath) {
            throw 'A Visual Studio installation with the x64/x86 C++ tools was not found.'
          }
          if (-not (Test-Path -LiteralPath $installer)) {
            throw "Visual Studio Installer was not found at $installer"
          }

          $arguments = @(
            'modify'
            '--installPath'
            "`"$installationPath`""
            '--add'
            'Microsoft.VisualStudio.Component.VC.ATL'
            '--quiet'
            '--norestart'
          )
          $process = Start-Process -FilePath $installer -WorkingDirectory $env:RUNNER_TEMP -ArgumentList $arguments -Wait -PassThru -WindowStyle Hidden
          if ($process.ExitCode -notin 0, 3010) {
            throw "Visual Studio Installer exited with code $($process.ExitCode)."
          }

          $atlHeader = Get-ChildItem -Path (Join-Path $installationPath 'VC\Tools\MSVC\*\atlmfc\include\atlbase.h') -File |
            Sort-Object FullName -Descending |
            Select-Object -First 1
          if (-not $atlHeader) {
            throw 'atlbase.h was not found after installing Microsoft.VisualStudio.Component.VC.ATL.'
          }
          Write-Output "ATL header: $($atlHeader.FullName)"

      - name: Set up Visual Studio 2022
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: amd64

      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MSYS
          location: 'D:\msys64'
          update: true
          path-type: inherit
          cache: true
          install: >-
            base-devel
            autoconf
            automake
            bison
            flex
            gawk
            gettext
            gperf
            make
            nasm
            patch
            perl
            perl-Archive-Zip
            perl-Font-TTF
            pkgconf
            unzip
            which
            zip
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-binutils

      - name: Expose native ninja on the Windows PATH
        shell: msys2 {0}
        run: |
          set -eux
          # Meson (a native Windows process, launched via the MSYS make) needs
          # its Ninja backend on the Windows PATH. Initial Windows-PATH entries
          # survive the MSYS->native conversion, but an entry appended to the
          # MSYS PATH inside a later step does not reliably reach the native
          # meson process. So copy the native ninja into a dedicated directory
          # that holds only ninja.exe (no shadowing of MSYS/VS tools) and add
          # that directory to the Windows PATH via GITHUB_PATH for all steps.
          if [ ! -x /mingw64/bin/ninja.exe ]; then
            echo "ERROR: native ninja not found at /mingw64/bin/ninja.exe (was mingw-w64-x86_64-ninja installed?)" >&2
            ls -la /mingw64/bin 2>&1 | grep -i ninja || true
            exit 1
          fi
          ninja_dir="$(cygpath -u "$RUNNER_TEMP")/ninja-bin"
          mkdir -p "$ninja_dir"
          cp /mingw64/bin/ninja.exe "$ninja_dir/ninja.exe"
          # Redundant fallback: also place ninja in /usr/bin, a directory the
          # MSYS2 setup already puts on the Windows PATH, in case the
          # GITHUB_PATH dir does not propagate to the native meson process.
          cp /mingw64/bin/ninja.exe /usr/bin/ninja.exe
          echo "Native ninja: $("$ninja_dir/ninja.exe" --version)"
          echo "$(cygpath -w "$ninja_dir")" >> "$GITHUB_PATH"
          # NOTE: intentionally NOT exporting a NINJA environment variable.
          # Meson (mesonbuild/environment.py detect_ninja_command_and_version)
          # uses NINJA *exclusively* when it is set and does not fall back to a
          # PATH search, so a NINJA value the bundled Meson process cannot
          # resolve makes backend detection fail outright. Instead, the "Build
          # Meson prerequisites" step places this ninja inside the directories
          # that make up Meson's own PATH (see gb_Python_PRECOMMAND in
          # solenv/gbuild/platform/com_MSC_class.mk), where a plain PATH search
          # finds it.

      - name: Inspect Windows build prerequisites
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Get-PSDrive -PSProvider FileSystem

          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          if (-not (Test-Path -LiteralPath $vswhere)) {
            throw "vswhere.exe was not found at $vswhere"
          }
          & $vswhere -latest -products '*' -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath

          $compiler = Get-Command cl.exe -ErrorAction SilentlyContinue
          if (-not $compiler) {
            throw 'cl.exe was not found after setting up Visual Studio 2022'
          }
          $compiler | Format-List Source, Version

          $cmake = Get-Command cmake.exe -ErrorAction SilentlyContinue
          if ($cmake) {
            $cmake | Format-List Source, Version
          } else {
            Write-Warning 'cmake.exe is not currently on PATH; configure may locate the Visual Studio copy.'
          }

          $assemblyLinker = Get-Command al.exe -ErrorAction SilentlyContinue
          if (-not $assemblyLinker) {
            $sdkRoot = "${env:ProgramFiles(x86)}\Microsoft SDKs\Windows"
            if (Test-Path -LiteralPath $sdkRoot) {
              $assemblyLinker = Get-ChildItem -LiteralPath $sdkRoot -Filter al.exe -File -Recurse |
                Sort-Object FullName -Descending |
                Select-Object -First 1
            }
          }
          if (-not $assemblyLinker) {
            throw 'al.exe was not found in PATH or the installed Microsoft SDKs'
          }
          $assemblyLinkerPath = if ($assemblyLinker -is [System.IO.FileInfo]) {
            $assemblyLinker.FullName
          } else {
            $assemblyLinker.Source
          }
          Write-Output "al.exe: $assemblyLinkerPath"
          Split-Path -Parent $assemblyLinkerPath | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

          $sdkBin = Join-Path ${env:ProgramFiles(x86)} 'Windows Kits\10\bin'
          $installerTools = @('msiinfo.exe', 'msidb.exe', 'uuidgen.exe', 'msitran.exe')
          $installerToolsDirectory = Get-ChildItem -LiteralPath $sdkBin -Directory -Recurse |
            Where-Object {
              $candidate = $_.FullName
              -not ($installerTools | Where-Object { -not (Test-Path -LiteralPath (Join-Path $candidate $_)) })
            } |
            Sort-Object FullName -Descending |
            Select-Object -First 1
          if (-not $installerToolsDirectory) {
            throw "Windows Installer SDK tools were not found below $sdkBin"
          }
          Write-Output "Windows Installer SDK tools: $($installerToolsDirectory.FullName)"
          $installerToolsDirectory.FullName | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

          cmd /c 'fsutil 8dot3name query C:'

      - name: Configure LibreOfficeDev
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          chmod +x \
            .github/scripts/msys2-patch-wrapper.sh \
            .github/scripts/msys2-tar-wrapper.sh
          external_tar="$(cygpath -u "$RUNNER_TEMP")/lo-externaltar"
          mkdir -p "$external_tar"
          export PKG_CONFIG=/mingw64/bin/pkgconf.exe
          export PATCH="$PWD/.github/scripts/msys2-patch-wrapper.sh"
          export GNUTAR="$PWD/.github/scripts/msys2-tar-wrapper.sh"
          dotnet="$(cygpath -m 'C:\Program Files\dotnet\dotnet.exe')"
          "$dotnet" --version
          ./autogen.sh \
            --build=x86_64-pc-cygwin \
            --host=x86_64-pc-cygwin \
            --with-visual-studio=2022 \
            --disable-debug \
            --with-dotnet="$dotnet" \
            --without-java \
            --without-doxygen \
            --enable-odk \
            --with-lang=en-US \
            --with-package-format=msi \
            --disable-online-update \
            --with-external-tar="$external_tar" \
            2>&1 | tee autogen.log

      - name: Fetch external sources
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          # LibreOffice treats a non-empty MSYSTEM as its WSL helper mode.
          # Assign it on make's command line so recursive makes also see it empty.
          # Its MIDL rule also expects INCLUDE to be a make variable, not the
          # semicolon-separated environment value from the VS developer shell.
          # LibreOffice exports UCRTVERSION itself; keeping VS's UCRTVersion too
          # gives native MSBuild duplicate case-insensitive environment keys.
          unset MSYSTEM INCLUDE UCRTVersion
          # Keep LibreOffice's native compiler paths in Windows form when an
          # MSYS2 process launches the native gcc/g++ wrapper executables.
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          make MSYSTEM= fetch 2>&1 | tee fetch.log

      - name: Build Meson prerequisites
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          unset MSYSTEM INCLUDE UCRTVersion
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          # Exercise the bundled Meson project used by the MSVC build before
          # the long full build. Cairo and Pixman use native gbuild on MSVC.
          # LIBO_PATH_SEPARATOR=';' makes the recipe emit the semicolon
          # (Windows) separator that the native MinGW pkgconf expects when it
          # is handed a Windows-form PKG_CONFIG_PATH.
          # Meson's default backend is Ninja. The bundled Meson runs under the
          # bundled python with its PATH overridden by gb_Python_PRECOMMAND
          # (solenv/gbuild/platform/com_MSC_class.mk) to
          # "<instdir>/program:<cwd-at-make-parse-time>", so it never sees the
          # system PATH and no NINJA env var (a set-but-unresolvable NINJA makes
          # Meson fail without falling back to PATH). The first PATH entry is
          # instdir/program, but that directory does not exist yet at the start
          # of this step (it is created when the bundled python is installed
          # during this very build), so create it and place the native ninja
          # there for Meson's own PATH search to find. A copy in the lo-root is
          # kept as a fallback for the second PATH entry.
          cp /mingw64/bin/ninja.exe "$PWD/ninja.exe"
          mkdir -p instdir/program
          cp /mingw64/bin/ninja.exe instdir/program/ninja.exe
          # Also place an extensionless "ninja" in instdir/program so a PATH
          # search finds it even if the Meson process has an empty PATHEXT.
          cp /mingw64/bin/ninja.exe instdir/program/ninja
          echo "Native ninja placed at: $PWD/ninja.exe, instdir/program/ninja.exe and instdir/program/ninja"
          # Primary fix: set NINJA to the full path of the ninja we placed in
          # the lo root. Meson (detect_ninja_command_and_version) uses NINJA
          # exclusively when it is set, and a full path is resolved by checking
          # that exact file (bypassing the PATH directory search and PATHEXT
          # entirely). This is the most direct way to give Meson its Ninja
          # backend, independent of whatever PATH the bundled python actually
          # receives from gb_Python_PRECOMMAND.
          export NINJA="$(cygpath -w "$PWD/ninja.exe")"
          echo "NINJA environment variable set to: $NINJA"
          # The native ninja is a MinGW-w64 binary. Meson's process has a
          # restricted PATH (gb_Python_PRECOMMAND) that does NOT include
          # /mingw64/bin, so when Meson runs "ninja --version" the ninja
          # cannot load its runtime DLLs (libgcc_s_seh-1.dll, etc.) and the
          # version check fails -> "Could not detect Ninja". Copy the ninja's
          # DLL dependencies (from /mingw64/bin) into the same directories as
          # the ninja, so Windows resolves them from the executable's own
          # directory (first in the DLL search order).
          echo "=== native ninja DLL dependencies ==="
          /mingw64/bin/objdump.exe -p /mingw64/bin/ninja.exe 2>/dev/null | sed -n 's/.*DLL Name: //p' | sed 's/[[:space:]]*$//' | sort -u > "$PWD/ninja_dlls.txt"
          cat "$PWD/ninja_dlls.txt"
          for _dll in $(cat "$PWD/ninja_dlls.txt"); do
            if [ -f "/mingw64/bin/$_dll" ]; then
              cp -f "/mingw64/bin/$_dll" "$PWD/$_dll"
              cp -f "/mingw64/bin/$_dll" "instdir/program/$_dll"
            fi
          done
          # libstdc++-6.dll (a direct dep) transitively needs libwinpthread-1.dll,
          # which is NOT in the ninja's direct import table. Copy the full set of
          # MinGW runtime DLLs so the ninja (and its deps) can all load in Meson's
          # restricted environment.
          for _dll in libgcc_s_seh-1.dll libwinpthread-1.dll libstdc++-6.dll; do
            if [ -f "/mingw64/bin/$_dll" ]; then
              cp -f "/mingw64/bin/$_dll" "$PWD/$_dll"
              cp -f "/mingw64/bin/$_dll" "instdir/program/$_dll"
            fi
          done
          echo "ninja DLLs copied alongside the ninja (lo root + instdir/program)"
          # Capture the exact Meson PATH value the make computes for
          # gb_Python_PRECOMMAND, so a "Could not detect Ninja" can be attributed
          # to the real directories Meson searches.
          echo "=== Meson PATH value (computed for gb_Python_PRECOMMAND) ==="
          echo "first entry : $(cygpath -u "$PWD/instdir/program")"
          echo "second entry: $(cygpath.exe -uS 2>&1)"
          # Verify the ninja actually runs with a restricted native PATH that
          # matches Meson's environment (instdir/program + cwd, no /mingw64/bin),
          # confirming the DLL fix before the long build.
          echo "=== verify ninja runs in a Meson-like restricted environment ==="
          _verify_bat="$PWD/_ninja_verify.bat"
          {
            printf '%s\n' '@echo off'
            printf '%s\n' "set PATH=$(cygpath -w "$PWD")\instdir\program;$(cygpath -w "$PWD")"
            printf '"%s" --version\n' "$(cygpath -w "$PWD/ninja.exe")"
          } > "$_verify_bat"
          cmd //c "$_verify_bat" || echo "ninja FAILED to run in restricted env (missing DLLs?)"
          echo "=== end ninja DLL fix ==="
          # Diagnostic: capture the environment the bundled python (Meson) sees.
          # The meson.py tarball is extracted DURING this build (the meson dir
          # does not exist at the start of the step), so a hook placed into
          # meson.py at step start is overwritten by the extraction and never
          # runs. Instead, drop a sitecustomize.py (a new file the extraction
          # leaves intact) into both instdir/program (on Meson's PYTHONPATH)
          # and the pre-created meson dir (sys.path[0] when meson.py runs). If
          # the bundled python imports site, one of these writes the real
          # NINJA/PATH/PATHEXT/PYTHONHOME/PYTHONPATH to a file we read after
          # the build.
          {
            printf '%s\n' 'import os'
            printf '%s\n' 'try:'
            printf '%s\n' '    _base = os.path.dirname(os.path.abspath(__file__))'
            printf '%s\n' '    _p = os.path.join(_base, "ninja_env_dump.txt")'
            printf '%s\n' '    with open(_p, "a") as _f:'
            printf '%s\n' '        _f.write("=== meson env (sitecustomize) ===\n")'
            printf '%s\n' '        _f.write("CWD = " + repr(os.getcwd()) + "\n")'
            printf '%s\n' '        for _k in ("NINJA", "PATH", "PATHEXT", "PYTHONHOME", "PYTHONPATH"):'
            printf '%s\n' '            _f.write(_k + " = " + repr(os.environ.get(_k)) + "\n")'
            printf '%s\n' 'except Exception as _e:'
            printf '%s\n' '    try:'
            printf '%s\n' '        with open(os.path.join(os.path.dirname(os.path.abspath(__file__)), "ninja_env_dump.txt"), "a") as _f:'
            printf '%s\n' '            _f.write("sitecustomize error: " + repr(_e) + "\n")'
            printf '%s\n' '    except Exception:'
            printf '%s\n' '        pass'
          } > instdir/program/sitecustomize.py
          _meson_dir="workdir/UnpackedTarball/meson"
          mkdir -p "$_meson_dir"
          cp instdir/program/sitecustomize.py "$_meson_dir/sitecustomize.py"
          # Lightweight diagnostic: is NINJA in make's environment? A tiny
          # makefile echoes $NINJA; if the export reached make it prints the
          # value, else an empty bracket. gbuild make inherits this same
          # environment when the outer make re-invokes it.
          echo "=== make environment: NINJA ==="
          echo 'n: ; @echo "NINJA_IN_MAKE=[$(NINJA)]"' > "$PWD/_ninja_probe.mk"
          make -f "$PWD/_ninja_probe.mk" n 2>&1 | head -3
          set +e
          # Put NINJA on the make command line: GNU make always exports
          # command-line variable assignments to the child (gbuild make)
          # environment, so the bundled Meson python is guaranteed to see it.
          # Meson (detect_ninja_command_and_version) uses NINJA exclusively
          # when it is set, and a full path is resolved by checking that exact
          # file (bypassing the PATH directory search and PATHEXT entirely).
          # The ninja at that path has its MinGW runtime DLLs alongside it, so
          # "ninja --version" succeeds.
          make MSYSTEM= NINJA="$(cygpath -w "$PWD/ninja.exe")" LIBO_PATH_SEPARATOR=';' -j3 \
            ExternalProject_harfbuzz \
            2>&1 | tee meson-prerequisites.log
          rc=$?
          set -e
          # Print the captured environment even if the build failed, so a
          # "Could not detect Ninja" can be attributed to the exact PATH the
          # bundled python (Meson) saw. Then propagate the build's exit code.
          echo "=== ninja env dump (meson dir) ==="
          cat workdir/UnpackedTarball/meson/ninja_env_dump.txt 2>/dev/null || echo "(no dump in meson dir)"
          echo "=== ninja env dump (instdir/program) ==="
          cat instdir/program/ninja_env_dump.txt 2>/dev/null || echo "(no dump in instdir/program)"
          echo "=== instdir/program ninja check (after build) ==="
          ls -la instdir/program/ninja.exe instdir/program/ninja 2>/dev/null || echo "ninja NOT in instdir/program after build"
          echo "=== meson-log.txt (ninja detection details) ==="
          tail -30 workdir/UnpackedTarball/harfbuzz/builddir/meson-logs/meson-log.txt 2>/dev/null || echo "(no meson-log.txt)"
          cp -f workdir/UnpackedTarball/harfbuzz/builddir/meson-logs/meson-log.txt "$PWD/meson-log.txt" 2>/dev/null || true
          exit $rc

      - name: Build Firebird prerequisite
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          unset MSYSTEM INCLUDE UCRTVersion
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          make MSYSTEM= -j3 ExternalProject_firebird 2>&1 | tee firebird.log

      - name: Build NSS prerequisite
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          unset MSYSTEM INCLUDE UCRTVersion
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          # NSS embeds NSPR's legacy Windows makefiles. Build it early and
          # separately so path-conversion failures do not surface late in gbuild.
          make MSYSTEM= -j3 ExternalProject_nss 2>&1 | tee nss.log

      - name: Build bundled ZIP packages serially
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          unset MSYSTEM INCLUDE UCRTVersion
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          # Template and autocorrect archives use ZIP recipes that are unreliable
          # when several native zip processes start together under MSYS2.
          # LibreOffice's outer make invokes gbuild with PARALLELISM, so -j1 alone
          # does not serialize the inner make. Build the affected packages with
          # both controls before the parallel build.
          make MSYSTEM= PARALLELISM=1 -j1 \
            Package_extras_autocorr \
            Package_extras_tplpresnt \
            Package_extras_tplwizard \
            Package_extras_templates \
            Package_extras_tplwizdesktop \
            2>&1 | tee templates.log

      - name: Build LibreOfficeDev
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          unset MSYSTEM INCLUDE UCRTVersion
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          make MSYSTEM= -j3 2>&1 | tee build.log

      - name: Run VSDX export tests
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          unset MSYSTEM INCLUDE UCRTVersion
          export MSYS2_ENV_CONV_EXCL='REAL_CC;REAL_CXX;REAL_BUILD_CC;REAL_BUILD_CXX'
          make MSYSTEM= -j3 CppunitTest_sd_visio_export_tests 2>&1 | tee visio-tests.log

      - name: Remove build-only helpers from the install tree
        shell: msys2 {0}
        run: |
          set -eux
          # The "Build Meson prerequisites" step placed ninja (ninja.exe and an
          # extensionless copy) inside instdir/program and a sitecustomize.py
          # diagnostic into instdir/program and the meson dir so the bundled
          # Meson could find its Ninja backend. All Meson-driven builds are
          # done by now; these are build-time helpers, not part of the
          # product, so remove them before the install tree is verified and
          # packaged.
          rm -f instdir/program/ninja.exe instdir/program/ninja
          rm -f instdir/program/sitecustomize.py workdir/UnpackedTarball/meson/sitecustomize.py

      - name: Verify the portable program
        shell: pwsh
        run: |
          $soffice = Join-Path $env:GITHUB_WORKSPACE 'instdir\program\soffice.com'
          if (-not (Test-Path -LiteralPath $soffice)) {
            throw "Portable executable was not produced: $soffice"
          }
          & $soffice --headless --version
          if ($LASTEXITCODE -ne 0) {
            throw "Packaged soffice.com failed with exit code $LASTEXITCODE"
          }

      - name: Collect the Windows installer
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $installerDirectory = Join-Path $env:GITHUB_WORKSPACE 'artifacts\installer'
          New-Item -ItemType Directory -Path $installerDirectory -Force | Out-Null
          $installers = @(Get-ChildItem -Path (Join-Path $env:GITHUB_WORKSPACE 'workdir\installation') -Filter '*.msi' -File -Recurse)
          if ($installers.Count -eq 0) {
            throw 'The build completed without producing an MSI installer.'
          }
          $installers | ForEach-Object {
            Copy-Item -LiteralPath $_.FullName -Destination $installerDirectory -Force
          }
          Copy-Item -LiteralPath (Join-Path $env:GITHUB_WORKSPACE 'README.md') -Destination (Join-Path $installerDirectory 'README-VSDX-FORK.md')
          Get-ChildItem -Path $installerDirectory -Filter '*.msi' -File | Get-FileHash -Algorithm SHA256

      - name: Package the portable build
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $artifactDirectory = Join-Path $env:GITHUB_WORKSPACE 'artifacts'
          $bundle = Join-Path $env:RUNNER_TEMP 'LibreOfficeDev-VSDX-Windows-x64'
          $archive = Join-Path $artifactDirectory 'LibreOfficeDev-VSDX-Windows-x64.zip'
          New-Item -ItemType Directory -Path $artifactDirectory, $bundle -Force | Out-Null
          Copy-Item -Path (Join-Path $env:GITHUB_WORKSPACE 'instdir\*') -Destination $bundle -Recurse -Force
          Copy-Item -LiteralPath (Join-Path $env:GITHUB_WORKSPACE 'README.md') -Destination (Join-Path $bundle 'README-VSDX-FORK.md')
          Compress-Archive -LiteralPath $bundle -DestinationPath $archive -CompressionLevel Optimal
          Get-FileHash -Algorithm SHA256 -LiteralPath $archive

      - name: Upload the Windows portable build
        uses: actions/upload-artifact@v7
        with:
          name: LibreOfficeDev-VSDX-Windows-x64
          path: artifacts/LibreOfficeDev-VSDX-Windows-x64.zip
          retention-days: 14
          compression-level: 0
          if-no-files-found: error

      - name: Upload the Windows installer
        uses: actions/upload-artifact@v7
        with:
          name: LibreOfficeDev-VSDX-Windows-x64-MSI
          path: artifacts/installer/
          retention-days: 14
          compression-level: 0
          if-no-files-found: error

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: windows-vsdx-build-logs
          path: |
            autogen.log
            fetch.log
            meson-prerequisites.log
            workdir/UnpackedTarball/meson/ninja_env_dump.txt
            instdir/program/ninja_env_dump.txt
            meson-log.txt
            ninja_dlls.txt
            firebird.log
            nss.log
            templates.log
            build.log
            visio-tests.log
            config.log
            workdir/CppunitTest/sd_visio_export_tests.test.log
            workdir/CustomTarget/instsetoo_native/install/*.log
          if-no-files-found: ignore
          retention-days: 14
